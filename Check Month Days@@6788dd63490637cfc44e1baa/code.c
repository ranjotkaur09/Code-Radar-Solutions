#include <stdio.h>

int main(){
    int num;
    scanf("%d",&num);
    if(num is between 1 && 12){
        printf("the number of days in a month");
    }
    else{
        printf("Invalid month");
    }
    return 0;
}