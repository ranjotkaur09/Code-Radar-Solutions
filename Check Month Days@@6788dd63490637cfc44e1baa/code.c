#include <stdio.h>

int main(){
    int num;
    scanf("%d",&num);
    if(num is between 1 && 12){
        print("the number of days in a month");
    }
    else{
        print("Invalid month");
    }
    return 0;
}